fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
boto3>=1.35.0
python-magic>=0.4.27
Pillow>=11.0.0
PyPDF2>=3.0.0

# Optional adapters — imported lazily inside repo/ only when the matching
# provider is configured. The default trainer (simulated) and captioner
# (templated) need neither, so a fresh `pnpm dev` runs without them.
anthropic>=0.40.0
replicate>=1.0.0
# The real on-device trainer (TRAINER_PROVIDER=local) needs a heavy ML stack
# (torch, diffusers, ...). It is kept out of this default install; opt in with:
#   pip install -r requirements-local-trainer.txt

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
